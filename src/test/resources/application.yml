spring:
  datasource:
    url: jdbc:h2:mem:toolqueue-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  task:
    scheduling:
      enabled: false

